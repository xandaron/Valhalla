#+build !Windows
#+build !Linux
#+build !FreeBSD
#+build !OpenBSD
#+build !NetBSD
package ImGui_ImplGlfw

@(private = "package")
Data :: distinct DataBase

@(private = "package")
ViewportData :: distinct ViewportDataBase

